/*#ifndef DATA_IO_MACRO_H
#define DATA_IO_MACRO_H

#include <stdio.h>

#define PRINT_ARRAY(arr, size)
void a {
    for (int i = 0; i < (size); i++) {
        printf("%d ", (arr)[i]);
    }
    printf("\n");
}
#define PRINT_ARRAY_TYPE(arr, size, type)
void b {
    for (int i = 0; i < (size); i++) {
        printf("%" #type " ", (arr)[i]);
    }
    printf("\n");
}

#endif
*/